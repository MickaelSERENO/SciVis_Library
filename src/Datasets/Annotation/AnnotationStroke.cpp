#include "Datasets/Annotation/AnnotationStroke.h"

namespace sereno
{
    AnnotationStroke::AnnotationStroke(uint32_t rgbaColor, uint32_t strokeWidth) : m_rgbaColor(rgbaColor), m_strokeWidth(strokeWidth)
    {}
}
